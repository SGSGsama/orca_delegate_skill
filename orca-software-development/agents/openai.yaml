interface:
  display_name: "Orca Software Development"
  short_description: "Default Orca delegation for non-trivial coding"
  default_prompt: "Use $orca-software-development to delegate implementation and diagnosis through Orca, then review and verify the result."
policy:
  allow_implicit_invocation: true
